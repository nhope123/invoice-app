import { FC } from 'react';
import { ClientCardProps } from './types';

const ClientCard: FC<ClientCardProps> = (props) => {
  const { } = props;

  return <div>ClientCard Component</div>;
};
  
export default ClientCard;
  