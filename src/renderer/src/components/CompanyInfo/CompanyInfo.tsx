import { FC } from 'react';
import { CompanyInfoProps } from './types';

const CompanyInfo: FC<CompanyInfoProps> = (props) => {
  const { } = props;

  return <div>CompanyInfo Component</div>;
};
  
export default CompanyInfo;
  