interface CompanyInfoProps {
  name: string;
  street: string;
  city: string;
  province: string;
  postalCode: string;
  country?: string;
  phone?: string;
  email?: string;
};

export type { CompanyInfoProps };
